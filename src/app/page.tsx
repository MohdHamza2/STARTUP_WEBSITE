import { BrandStatement } from "@/components/sections/BrandStatement";

/**
 * Homepage.
 *
 * Section order follows prompt §6. Sections are added phase by phase; the hero
 * sequence (§7–§12) lands in Phase 3 and mounts above BrandStatement.
 */
export default function HomePage() {
  return (
    <>
      <BrandStatement />
    </>
  );
}
